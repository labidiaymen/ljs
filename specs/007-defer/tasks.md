# Tasks: defer

- [x] T1 Add `defer_stmt` to the AST.
- [x] T2 Parse `defer <stmt>;` and `defer { ... }`.
- [x] T3 Type-check the deferred body in the enclosing scope.
- [x] T4 Emit a Zig `defer { ... }` block.
- [x] T5 Valid example + manifest; `zig build conformance`.
